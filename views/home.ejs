<%- include("partials/header") -%>


<h1>Home</h1>
<p><%= contentHome %></p>

<% posts.forEach((post) => { %>
    <% let route = "/posts/"+ post.title.replace(/\s+/g, '-') %>
    <% console.log(route);%>

    <h2><%= post.title %></h2>
    <p>
        <%= post.body.slice(0, 100) + "..." %>
        <a href=<%=route%> >Read more</a>
    </p>

    <% }); %>

<%- include("partials/footer") -%>